# Product — Psycho

## brunelleschi_stage

- **Стадия:** POC B
- **Последнее обновление:** 2026-05-22

---

## project

- **Название:** Psycho
- **Тип:** Telegram-бот с локальным LLM, граф знаний в Obsidian
- **Краткое описание:** Личный бот-собеседник, который через серию вопросов извлекает из ответов пользователя принципы, ценности и убеждения, складывает их в граф концептов в Obsidian-vault и подсвечивает противоречия.

---

## purpose

Помочь владельцу построить и поддерживать живую карту собственного внутреннего мира: что я считаю правильным, что красивым, во что верю, как отношусь к работе/смерти/национальности и т.д. Карта строится не одним приёмом, а в режиме непрерывного диалога — бот задаёт точные вопросы, разбирает ответы на «атомы» (концепты), связывает их между собой и сам ловит моменты, когда новые высказывания противоречат старым.

Ценность — не сам граф, а **давление на собственные взгляды**: систематические вопросы заставляют формулировать то, что обычно не формулируется, а ловля противоречий показывает места, где мысль сама с собой не сходится.

---

## audience

**Пользователи и роли:**

- **Владелец/админ** — `OWNER_TELEGRAM_ID`. Видит свою базу, плюс админ-команды `/adduser`/`/removeuser`/`/users`.
- **Доверенные гости** — несколько добавленных по `user_id` (env `ALLOWED_TELEGRAM_IDS` + рантайм `.psycho/users.json`). У каждого — **изолированная** база в `users/<id>/`; гость не видит чужие данные, владелец не видит гостевые из бота (они в отдельных папках). При первом обращении — disclaimer о приватности.
- Все, кто не в whitelist, — боту молчат. Подсказки команд — только доверенным (`BotCommandScopeChat`); админ-команды — только владельцу.

**Jobs-to-be-done:**

AJTBD от внешнего аналитика не поступал. Фиксируем то, что есть в коде и истории:

- Регулярно (раз в день — `DAILY_HOUR`) получать вопрос на одну из 10 тем и отвечать в свободной форме.
- В любой момент инициировать сессию: `/ask` (на выбор бота / на выбранную тему / `/ask <свободный текст>` как затравку), `/echo <вопрос>` (собственный вопрос, метка «пользовательский», со своим Q-номером).
- Скинуть свободную заметку `/ucho <текст>` — сохраняется в `notes/` и разбирается в граф.
- Возвращаться к старому вопросу: `/requestion N` (задать заново, мгновенно) или Telegram-**reply** на любое сообщение прошлой сессии (продолжит её — кольцо 25 сессий).
- Посмотреть свой портрет: `/about` — отформатированный текст о том, каким бот тебя видит; дальше обычная сессия-обсуждение.
- История: `/history` — последние 25 заданных вопросов (без ответов).
- Проверить, что бот жив, не прерывая ожидаемую реакцию: `/pebble` → «буль.» (не трогает сессию). Список команд: `/help`. Закрыть сессию (смыв, без потери данных): `/start`.
- Видеть результат в Obsidian: Graph View с фильтром `path:concepts/`, отдельные `.md` концептов с callout-блоками, MOC-файлы по доменам.

---

## context

- **Домен:** личный самоанализ + ведение Obsidian-vault как «второго мозга».
- **Бизнес-окружение:** проект не клиентский. Один пользователь, своя машина (Ryzen 5 5600 / 32 GB / RTX 3060 12 GB), Obsidian-vault в YandexDisk-папке для синка между устройствами.
- **In scope:** диалог через Telegram, локальный LLM-разбор ответов, запись концептов/связей/конфликтов в vault, Obsidian-нативный формат (callouts, aliases, block refs, per-domain MOC), 10 предметных доменов.
- **Out of scope:** многопользовательский режим, веб-интерфейс, монетизация, экспорт в другие форматы, голосовые сообщения, картинки, любая телеметрия наружу.
- **Допущения:** владелец читает результат в Obsidian, а не в боте; правки в Obsidian руками — допускаются и не должны теряться при последующих автозаписях бота.
- **Риски:**
  - Vault лежит в YandexDisk-папке — фоновый синк может конкурировать с записями бота (закрыто atomic writes + manifest drift detection).
  - Локальная LLM на GPU 12 GB — Qwen 2.5 14B иногда галлюцинирует структуру JSON; закрывается строгой валидацией и фолбэками с логом.
  - Один пользователь = один источник правды; бэкап графа сейчас опирается на git внутри vault + YandexDisk-history.

---

## style

- **Персона:** бот говорит голосом **Иуды из Кариота** (по повести Андреева) — двуликий, вкрадчиво-колкий собеседник, который любит человека и потому не верит его красивым словам. Не «коуч», не «дневник»: подсовывает зеркало, нащупывает зазор между тем, каким человек себя считает, и тем, каков он есть. Без эмодзи в ответах LLM, без воды, без морали и утешения.
- **UI-стиль:** Telegram нативный, минимум разметки. Заголовок вопроса в формате `Q<N> · [mode ·] _domain_`, тело — `<code>…</code>` (для long-press копирования). Индикатор «Думаю» (один стикер 🎰 + текст) — только при генерации вопроса (`/ask`) и портрета (`/about`); реакции в диалоге идут молча.
- **Тон коммуникации:** на «ты», провокативно-интимный. Бот поддевает, сомневается вслух, бьёт по самооправданиям — но не выдумывает противоречий там, где их нет.
- **Терминология:** «концепт» (узел графа), «тема» (предметная область = поле `domain`; на графе — цвет узла + тег-хаб `#ТЕМА` заглавными, напр. `#AESTHETICS`), «сквозная тема» (русский тег-мотив, связывает концепты через разные домены, напр. `#присутствие`), «связь» (`supports/contradicts/derived_from/related`), «алиас» (альтернативная формулировка), «raw» (сырой Q&A-лог дня), «MOC» (оглавление темы; на графе скрыт — его роль хаба берёт тег).

---

## features

### Core

- **10 тем вопросов:** ethics, aesthetics, politics, everyday, relationships, identity, mortality, nationality, knowledge, work. Выбор темы через inline-кнопки или аргумент команды; «на выбор бота» = равномерный random.
- **Модель диалога (открытая сессия-обсуждение):** главный вопрос **открывает сессию**; на каждый ответ бот даёт **реакцию-укол от первого лица** (НЕ вопрос — уточняющих вопросов бот не задаёт) и ждёт следующего сообщения. Сессия закрывается **только** когда задан новый главный вопрос (`/ask`/дневной/`/echo`/`/requestion`) или выполнена любая команда; всегда ≤1 открытой сессии. Любую из **последних 25 сессий** можно продолжить, ответив (reply) на любое её сообщение — снапшоты в кольце `_sessions.json`.
- **Многоликий Иуда (настроение → лицо):** на каждый ответ бот сперва дешёвым LLM-вызовом классифицирует настроение человека по шкале **PAD** — валентность / энергия / **доминирование** (контроль ↔ бессилие) + направленность + фоновая эмоция. Код считает вектор по сессии (recency на последнем сообщении + затухающий prior из портрета) и **выбирает контрастное «лицо»** (раскачать вялого, осечь властного/яркого, поддержать придавленного) из закрытого списка (раскачивание, насмешка, давление на больное, ласка, вера, клятва, …); ось доминирования здесь приоритетна. Лицо применяется и к реакциям, и к вопросам. Пары `настроение→лицо` копятся в `_mood_log.jsonl`. В классификации арбитру-LLM подаётся инструментальная подсказка — нативная русская **VAD-оценка** из лексикона **NRC-VAD** (`bot/lexicon.py`, лемматизация `pymorphy3`, офлайн, без перевода и без облака); LLM может её перебить (ирония/сарказм). Реплики Иуды очищаются от пунктуации, кроме `. , ?` (стиль персоны). **Анализ настроения выполняется только для владельца (OWNER)** — как персональный инструмент-инсайт; владельцу он приходит **отдельным сообщением перед основным ответом** (эмоция, валентность/энергия/доминирование, направленность, устойчивость, выбранное лицо). Для остальных доверенных настроение не анализируется — лицо выбирает сама LLM.
- **Capture-first разделение труда:** бот (Qwen 14B) только *захватывает* — создаёт **черновые** концепты `status: draft` без связей и без конфликтов. Выверенный граф (промоушн `draft → stable`, связи, реальные противоречия, дедуп, profile, MOC, теги, русские имена) строит сильная модель (Claude) раз в неделю через скилл `weekly-review`. Причина: Qwen слаба в графовом рассуждении — плодит дубли и ложные конфликты.
- **LLM только анализирует — запись в БД делает код:** в режиме `process` модель возвращает лишь `observations` (что человек выдал о себе: `domain/type/name/summary/quote`) и следующий вопрос. Она НЕ придумывает slug, НЕ решает create-vs-update, НЕ пишет raw. Всё это детерминированно в коде: raw пишется дословно, slug выводится из имени (транслит), дубли ловит дедуп. Это снижает нагрузку на локальную модель и повышает надёжность JSON (важно под стилизованной персоной).
- **Свободные заметки (`/ucho`):** verbatim в `notes/<дата>.md` + прогон через тот же `process` — черновые концепты попадают в граф (заметка работает как ответ без заданного вопроса).
- **Дедупликация (live, лёгкая, в коде):** для каждого `observation` код прогоняет `resolve_slug` (по имени → по выведенному slug → aliases) + Jaccard-overlap на биграммах summary (0.7) против существующих в домене. Совпадение → дописать evidence + alias, не создавать дубль. Глубокий дедуп/слияние — у Claude еженедельно.
- **Портрет (`/about`):** отформатированный отдельным промптом текст «каким я тебя вижу» (из `about_user.md`), затем обычная сессия-обсуждение. Поглотил прежний `/review` (отдельной команды разговора о базе больше нет).
- **Постоянство:** активная сессия, сквозной счётчик Q-номеров и недоработанный ответ (`pending_answer`) переживают рестарт контейнера (`_session.json`, `_state.json`, atomic writes). Офлайн-ответы доезжают из очереди Telegram при следующем старте polling.
- **Self-check при старте:** механический (без LLM) прогон — MOC rebuild по всем доменам, валидация связей, отчёт о дублях/сиротах в `.psycho/startup-check.md`.
- **История и повтор:** `/history` показывает последние 25 заданных главных вопросов (без ответов); `/requestion N` задаёт выбранный заново.
- **Продолжение прошлых разговоров (reply-resume):** reply (смахни сообщение) на любую реплику одной из **последних 25 сессий** оживляет её с того места. Снапшоты сессий — кольцо `_sessions.json`. Карта `message_id→вопрос` (`qmap`, последние 50) служит фолбэком и реконструкцией.
- **Ежедневный пинок:** APScheduler шлёт главный вопрос в `DAILY_HOUR` — **ровно один раз в день** на пользователя (общий дедуп по дате `last_daily_date` для cron, `/dailyall` и догона). Активная сессия и прошлые ответы НЕ блокируют (как `/ask`). Если бот лежал в час рассылки — дневной **досылается позже в тот же день** при старте (за прошлые дни — нет).

### Secondary

- **Obsidian-native рендеринг:** callouts `[!summary]/[!quote]/[!question]/[!contradiction]/[!source]`, aliases во frontmatter, block refs `[[raw/<date>#^Q<n>]]` (точная навигация в raw).
- **Граф настроений (строит weekly-review):** отдельная папка `mood/` — узлы-настроения человека (quality) и узлы-лица Иуды, рёбра `настроение→лицо` с характеристикой (терпимо/нейтрально/отвергнуто + счётчик). Из накопленных пар сильная модель выводит per-user карту `_mood_map.json` (какое лицо человеку терпимо), `mood_baseline` (фон настроения) и `user_prompt.md` — сжатую подстройку персоны под человека (идёт в системный промпт; пишет только weekly).
- **Наглядный русский граф (строит weekly-review):** у выверенных (`stable`) концептов имя файла = `slug` = русский заголовок — узлы графа подписаны по-русски (не транслитом). Все ссылки ведут по русскому имени; старый ascii-slug уходит в `aliases`. Узлы окрашены по домену (10 цветовых групп), узлы-теги серые. Бот не перезатрёт такой концепт — `safe_slug` принимает только ASCII, русский slug служит естественным write-barrier.
- **Теги как второе измерение графа:** на концепте — доменный тег `#ТЕМА` (= имя папки заглавными, хаб домена) + 1–3 **сквозные** русские темы из закрытого реестра `_tags.md`. Сквозные темы связывают концепты ЧЕРЕЗ разные домены (то, чего не дают папки и ссылки). Графовые настройки (фильтр только `concepts`, скрытие MOC-файлов, цвета, серые теги) лежат в `.obsidian/graph.json`; для нового пользователя бот сидит их из шаблона `bot/assets/graph.json`.
- **Per-domain MOC:** `concepts/<domain>/<DOMAIN>.md` — оглавление темы, концепты сгруппированы по type, пересобирается после каждого изменения. На графе **скрыт** фильтром (`-file:<DOMAIN>`) — роль хаба домена берёт тег `#ТЕМА`.
- **Защита ручных правок:** если файл концепта правился в Obsidian (или YandexDisk pull-нул внешнюю правку), бот **не перезаписывает** — записывает warn в `.psycho/log.md` и сохраняет правки пользователя.
- **Операционный лог:** `.psycho/log.md` append-only, фиксирует drift-skip, dedup, sanitize, LLM-фолбэки.
- **Спиннер:** анимированный dice + текстовый placeholder во время ожидания LLM. Placeholder удаляется по готовности, dice остаётся как маркер «здесь думал».

### Out of scope

- Открытый/публичный multi-user, регистрация, MFA, 152-ФЗ-режим — нет. Multi-user есть, но **только для нескольких доверенных** по whitelist (владелец добавляет вручную); у каждого изолированная база. Открытый доступ и связанный с ним hardening — MVP B.
- Веб-интерфейс / мобильное приложение — клиент только Telegram, чтение результата только в Obsidian.
- Облачный LLM (OpenAI/Anthropic) — приватность приоритетна, всё локально через Ollama.
- Голосовые сообщения, картинки, файлы — только текст.
- Экспорт графа в другие форматы (Notion, Roam, JSON-API) — Obsidian-vault считается source of truth.
- Бэкапы по cron, мониторинг, дашборды — это обвязка MVP A, на PoC B не делаем.
- Миграция исторических концептов 4→10 доменов — есть отдельный скрипт `scripts/migrate_domains.py`, но он не запущен; до запуска старые концепты в legacy-доменах продолжают читаться.

---

## scenarios

Как пользователь взаимодействует с системой через чат. Сначала штатные сценарии, в конце — edge cases (что происходит на сбоях и нестандартном вводе).

### Доступ (на каждом сообщении)

- **Чужой (не в whitelist)** — бот молчит, команды в меню ему не видны.
- **Доверенный гость** — полный базовый набор команд, своя изолированная база.
- **Владелец** — базовый набор + админ-команды.
- Любая команда, **кроме `/pebble`, закрывает активную сессию** перед выполнением (отправил команду посреди разговора — оборвал текущую сессию; данные целы).

### Основной цикл «вопрос → ответ → реакция»

1. Приходит главный вопрос (`/ask`, дневной в `DAILY_HOUR`, `/echo`, `/requestion`).
2. Пользователь отвечает свободным текстом.
3. Бот пишет сырой Q&A и черновые концепты, затем роняет **реакцию-укол** от первого лица (не вопрос).
4. Сессия **остаётся открытой**: реакция — якорь следующего хода, ответ на неё снова разбирается. Уточняющих вопросов бот не задаёт принципиально.

### Задать вопрос

- **`/ask`** без аргумента — выбор темы inline-кнопками (10 тем + «на выбор бота»).
- **`/ask <тема>`** (тема из списка 10) — вопрос внутри названной темы.
- **`/ask <свободный текст>`** — текст становится **затравкой/контекстом** для генерации: бот целит вопрос в то, о чём попросил человек, домен подбирает сам. Это не ответ, а заявка на разговор.
- **`/echo <вопрос>`** — пользователь сам формулирует вопрос; бот возвращает его с меткой «пользовательский» и **присваивает Q-номер** (виден в `/history`, доступен для `/requestion`). LLM при этом не зовётся.
- **`/requestion N`** — задать сохранённый главный вопрос Q*N* заново, мгновенно, без LLM.

### Свободный ввод

- **Ответ на текущий вопрос** — обычный текст → разбор и реакция.
- **Reply на сообщение** — оживляет ту сессию из кольца **последних 25** и продолжает её с места.
- **`/ucho <текст>`** — свободная заметка: сохраняется verbatim в `notes/` и прогоняется через тот же разбор (работает как ответ без заданного вопроса).

### Портрет и сервис

- **`/about`** — текст «каким я тебя вижу» из портрета, затем обычная сессия-обсуждение (можно отвечать reply). Портрет пуст → честно «ещё не распробовал, поговори».
- **`/history`** — последние 25 заданных главных вопросов (без ответов).
- **`/pebble`** — «буль.» (проверка живости), **не трогает сессию** — единственная такая команда.
- **`/help`** — подсказка по командам. **`/start`** — смыв (закрыть сессию, данные целы).

### Админ (только владелец)

- **`/adduser <id>` / `/removeuser <id>` / `/users`** — управление доступом (без LLM). `/removeuser` убирает доступ, но папку данных оставляет.
- **`/dailyall`** — немедленная рассылка дневного вопроса всем; пропускаются те, кому он уже ушёл сегодня (общий дедуп по дню с cron/догоном), отчёт «отправлено / пропущено (уже было сегодня) / ошибок».

### Настроение и лицо

- Вялый/ровный/скучающий ответ → бот **раскачивает** (подначка, насмешка).
- Яркий/самоуверенный/высокомерный → **осекает** (сомнение, давление на больное, холод).
- Хрупкий/придавленный/тревожный → **поддерживает** (ласка, вера, вселение уверенности, клятва).
- Настроение копится по сессии (день может тянуться) — вес у последнего сообщения, фон берётся из портрета. Со временем weekly-review подстраивает выбор лица под то, что человеку терпимо.

### Edge cases

- **Занят (rate limit):** один активный LLM-вызов на пользователя + cooldown; второй запрос подряд → сообщение «занят».
- **LLM упал / таймаут / не-JSON:** нейтральная фраза пользователю, stacktrace в лог, граф не портится; неизвестный домен от модели коэрсится в `everyday`.
- **Рестарт посреди ответа:** двухфазный коммит (`pending_answer` на диск до LLM) — на старте ответ дожимается, задвоения нет.
- **Бот лежал — офлайн-бэклог:** входящий текст одного пользователя **склеивается в один ответ** (один итоговый укол, а не на каждое сообщение); команды переигрываются по одной.
- **Пустой / слишком длинный ввод:** пусто после очистки → просьба переформулировать (LLM не зовётся); длинное — обрезается с предупреждением.
- **Reply на старую сессию (выпала из кольца 25):** фолбэк-цепочка `qmap` → реконструкция из текста сообщения → трактовка как заметка.
- **Несколько пользователей:** полная изоляция данных (`users/<id>/`), один git-коммит = данные одного пользователя.
- **Дедуп на лету:** повтор концепта (по имени / Jaccard) дописывает evidence, новый узел не плодит.
- **Write-barrier:** выверенные `stable`-концепты (русский slug) бот физически не перезаписывает; новые цитаты к ним доносит weekly-review. Бот никогда не удаляет файлы.

---

## quality

**Нефункциональные требования:**

- **Приватность:** ни один токен ответа пользователя не покидает локальную машину. Все LLM-вызовы идут через Ollama по `OPENAI_BASE_URL=http://ollama:11434/v1`.
- **Сохранность данных:** ручные правки в Obsidian не должны теряться при последующей автозаписи. Закрыто manifest+mtime drift detection.
- **Целостность под падением:** kill контейнера в момент записи не должен оставить битый JSON / полу-записанный концепт. Закрыто atomic writes (tmp + os.replace).
- **Откат на ошибку:** падение посреди `_apply_processed` должно откатывать vault к pre-state. Закрыто `git_wrap` транзакцией (pre/post commit, на исключение `git reset --hard`).
- **Latency:** на RTX 3060 + Qwen 14B главный вопрос генерируется за 5–20 сек, обработка ответа 10–30 сек. На CPU (Ryzen 5 5600) с Qwen 7B — 5–15 сек на вопрос, терпимо.

**Метрики успеха (для PoC B):**

- За неделю владелец отвечает ≥10 вопросов без падения бота.
- Граф в Obsidian-Graph-View визуально читается: видны кластеры по доменам, цветные связи между концептами.
- Ни один автоматический коммит бота в vault не разрушил ручную правку.
- Минимум один conflict-callout записан и помог осознать противоречие в собственной позиции.

---

## rules

### Hard

- LLM-вызовы только локально. Никаких облачных провайдеров.
- Все команды и подсказки видны только владельцу.
- Vault не теряет ручных правок: drift → skip + log, не overwrite.

### Firm

- Концепты — атомарные. Один концепт = одна идея, не лонгрид.
- **Slug выводит код, не LLM.** Черновик: ascii-slug из имени через `slugify` (транслит). Выверенный (`stable`, weekly-review): имя файла = slug = русский заголовок (наглядный граф), старый ascii-slug — в `aliases` (якорь дедупа + back-compat ссылок).
- Парсер обратимо понимает старый формат (H2-секции) — на случай старых концептов и ручного редактирования.

### Soft

- В Telegram-сообщениях минимум разметки. Заголовок коротко, тело — `<code>`.
- LLM-фолбэки (неизвестный домен, кривой type/status) — логируем, не падаем.

---

## accept

PoC B считается пройденным, когда:

- Бот запускается одной командой (`docker compose up -d`) и не падает в течение недели.
- Все 10 доменов реально работают: `/ask <domain>` для каждого даёт осмысленный вопрос, ответ корректно разбирается LLM, концепты появляются в `concepts/<domain>/`.
- Открытие MOC-ноды `<ТЕМА>.md` в Obsidian показывает все концепты темы, сгруппированные по type.
- Open Graph View с фильтром `path:concepts/` визуально читается — узлы и связи на месте.
- Конфликт-callout (`[!contradiction]`) реально записан хотя бы раз и виден в Reader-режиме Obsidian.
- `.psycho/log.md` существует, наполняется и не пустой.
- При ручной правке `.md` в Obsidian → следующий ответ не перезатёр правку.

---

## notes

### Продуктовые решения

- **2026-05-21:** **ответ на конкретный вопрос через reply / `/answer N`.** Закрывает сценарий «бот задал вопрос → пользователь выполнил команду → вопрос остался без ответа, но терять его не хочется». Теперь reply на сообщение-вопрос (или `/answer N <текст>`) открывает probe-сессию, заякоренную на этот вопрос. Решения по развилкам: `/answer` — только инлайн (`/answer N <текст>`, без текста — подсказка); поясняющие вопросы (клариферы) отвечаемы через reply наравне с главными; смена вопроса молча закрывает текущую сессию; первый ответ на неотвеченный — под исходным Q-номером, повторный на отвеченный — Q-повтор; карта вопросов — последние 50, вытесненный вопрос даёт мягкий отказ. Команда `/answer` ранее (см. ниже) была убрана как дубль `/requestion` — здесь возвращается с **другим** смыслом: не «задать заново», а «ответить на заданное».
- **2026-05-21:** персона бота — **«Иуда из Кариота»** (по Андрееву). Промпты (`system/review/summarize` + `seeds`) переписаны в этот голос: двуликий провокатор, подсовывает зеркало, бьёт по самооправданиям. Механика не изменилась — характер живёт в формулировках вопросов/комментариев, JSON-контракт строгий.
- **2026-05-21:** **наглядный русский граф + теги.** У `stable`-концептов имя файла = slug = русский заголовок (узлы графа по-русски); ссылки ведут по имени, ascii-slug — в `aliases`. Добавлены теги: доменный `#ТЕМА` (CAPS, хаб) + сквозные русские темы (реестр `_tags.md`, связывают концепты через домены). Граф: цвет по домену, серые теги, MOC-файлы скрыты. Практики — `kepano/obsidian-skills` + praxis. Строит weekly-review; настройки графа сидятся новым юзерам из шаблона `bot/assets/graph.json`.
- **2026-05-21:** `process` переведён на **«LLM только анализ»**. Модель отдаёт `observations` (`domain/type/name/summary/quote`) + вопрос; всю запись и идентичность (raw дословно, slug из имени, дедуп, create-vs-update, верификация цитаты) делает код. Цель: меньше нагрузка на локальную Qwen и надёжнее JSON под стилизованной персоной. Классификация и вопросы остаются за LLM — качество не снижено.
- **2026-05-21:** добавлен **multi-user для доверенных** (изоляция данных по `users/<id>/`, whitelist + роли, consent-онбординг). Формально multi-user — обязательство MVP A; сознательно остаёмся POC B по духу и берём только изоляцию + whitelist для пары доверенных, без полного MVP-A hardening (rate limiting, бэкапы по cron, дашборд) — он отложен до реальной альфы. Открытый доступ — out of scope (MVP B). Бот по-прежнему не удаляет данные: `/removeuser` убирает доступ, но папку `users/<id>/` оставляет.
- **2026-05-21:** разделение труда моделей зафиксировано как **capture-first**. Слабая локальная Qwen 14B отвечает только за live-захват (диалог, raw, **черновые** концепты `draft` без связей/конфликтов). Весь «умный» граф — связи, реальные противоречия, дедуп/слияние, переписывание profile, осмысленный MOC — строит сильная модель (Claude) раз в неделю через скилл `weekly-review` (proposal → apply под git). Причина: Qwen в реальном диалоге выдумывала ложные противоречия (пользователь их отвергал) и плодила дубли. Live-фича, которую слабая модель делает плохо, заменена на честную еженедельную от сильной. Embedding-модель для дедупа/поиска (`nomic-embed-text` в Ollama) сознательно отложена до MVP A.
- **2026-05-21:** ревизия набора команд. Убраны `/discuss` (оппонирование — не нужно), `/answer` (дублировал повтор вопроса), `/end` (слит со `/start`). `/requestion`(старый, «свой вопрос») → `/echo`; `/retry` → `/requestion` («повторить вопрос»); `/ping` → `/pebble` («буль.»). Добавлены `/ucho` (свободная заметка + разбор в граф), `/help` (список команд). `/start` стал «кнопкой смыва» — закрывает активную сессию, данные целы. Деструктивных команд (удаление графа) не заводим — бот никогда не удаляет (чистка вручную в Obsidian).
- **2026-05-21:** при старте контейнера — механический self-check без LLM (MOC rebuild + валидация связей + отчёт о дублях/сиротах). Глубокий смысловой реиндекс остаётся ручным через сильного агента (skill `weekly-review`), т.к. локальная Qwen 14B для реструктуризации слаба.
- **2026-05-20:** граф рендерится в Obsidian-native формате (callouts/aliases/block refs/MOC) вместо самодельных H2-секций. Источник — `kepano/obsidian-skills` + best practices глобального скилла praxis. Этап 2 плана `vast-inventing-raccoon.md`.
- **2026-05-20:** dedup концептов — Jaccard на биграммах summary, не BM25. Объём графа ≤ сотни узлов, простой алгоритм достаточен. Этап 3.
- **2026-05-20:** домены расширены с 4 до 10. Старые концепты в 4-доменной схеме мигрируются отдельным скриптом `scripts/migrate_domains.py`, двухфазно (proposal → apply через LLM-классификатор).

### Открытые вопросы

- Нужны ли «soft delete» концептов (LLM считает что концепт устарел) или достаточно ручного удаления в Obsidian? Сейчас бот никогда не удаляет файлы.
- Имеет ли смысл «daily review» — не вопрос, а сводка по последним концептам с просьбой подтвердить/опровергнуть? На PoC B пока нет.

### Журнал изменений

- **2026-05-22:** **портрет богаче — +6 измерений (8 → 14 секций).** Добавлены «Стиль» (вкус/самоподача), «Страсти — что вдохновляет», «Огорчает / разочаровывает», «Эпистемический стиль» (как познаёт мир), «Привязанность и дистанция» (как строит близость), «Ритуалы и быт» (уклад). Бот накапливает по ним короткие live-подсказки (`style/passion/letdown/epistemics/attachment/routine`), связную прозу пишет weekly-review.
- **2026-05-22:** **анализ настроения — только для владельца, отдельным сообщением.** Пайплайн настроения (классификация, вектор по сессии, выбор лица, запись в портрет/журнал) теперь запускается **только для OWNER**; для остальных доверенных лицо выбирает сама LLM. Владельцу разбор приходит **отдельным сообщением перед основным ответом** (эмоция, V/A/D, направленность, устойчивость, лицо) — не вопрос, шлётся мимо `_send_question`/qmap.
- **2026-05-22:** **ось доминирования + нативный русский сигнал настроения.** Шкала настроения расширена до **PAD** — добавлена ось «контроль ↔ бессилие» (`dominance`): классификатор её отдаёт, код считает по сессии, выбор лица использует приоритетно (формализует политику «властного — осади, придавленного — поддержи»). Инструментальная подсказка переведена с VADER-по-переводу на нативный русский **VAD-лексикон NRC-VAD** (`bot/lexicon.py` + `pymorphy3`, лексикон вшит в образ). Удалены перевод RU→EN (Argos), `vaderSentiment` и build-шаг модели — образ собирается быстрее, текст человека в перевод больше не гоняется. Всё локально, приватность сохранена.
- **2026-05-22:** **многоликий Иуда + граф настроений.** Двухвызовный пайплайн: LLM классифицирует настроение (категориально), код считает вектор по сессии (recency + prior из портрета, устойчивость через дисперсию) и выбирает контрастное лицо (`bot/moods.py`). Лицо — в реакциях и вопросах. Пары `настроение→лицо` → `_mood_log.jsonl`; weekly-review строит граф `mood/`, карту `_mood_map.json`, `mood_baseline` и per-user `user_prompt.md`. Портрет/`/about` больше не упоминают роль (владелец/гость) — только слова человека. Реплики Иуды чистятся от пунктуации, кроме `. , ?`. Всё локально, приватность сохранена.
- **2026-05-22:** добавлена секция `## scenarios` (сценарии взаимодействия пользователя с системой + edge cases). `/ask <свободный текст>` теперь принимает текст как затравку/контекст для генерации вопроса (раньше — только тема из списка или отказ). Уточнено, что `/echo` присваивает вопросу Q-номер. Промпты перестроены: персона вынесена в `iuda.md`, механика графа в `base.md`, удалён неиспользуемый `summarize`, `seeds` → `questions_examples` (подмешивается в генерацию вопроса по теме); `/about` берёт голос из `iuda.md`.
- **2026-05-20:** документ создан под Vibe++ после завершения этапов 1-3 плана `vast-inventing-raccoon.md`.
