# Тяжёлые стабильные зависимости — отдельный слой Docker-кэша (ставятся РАНЬШЕ
# requirements.txt). Меняются редко, поэтому добавление новых либ в requirements.txt
# не перекачивает их заново. Менять этот файл — только при апгрейде ядра.
aiogram==3.13.1
openai==1.59.5
APScheduler==3.10.4
python-dotenv==1.0.1
PyYAML==6.0.2
# Валидация контракта JSON-ответа LLM (наблюдения). Приходит транзитивно с
# openai, но пиннингуем явно — модель контракта в bot/llm.py зависит от pydantic v2.
pydantic==2.9.2
