# Дополнительные / новые зависимости (быстрый слой). Базовые тяжёлые ядро-либы — в
# requirements-base.txt (ставятся раньше и кэшируются отдельно). Сюда — лёгкие/часто
# меняемые либы.
# Лемматизация русского ввода для VAD-лексикона (bot/lexicon.py): нативно по-русски,
# CPU, офлайн. Заменяет связку перевод(argos)→VADER. dicts-ru — словарная база.
pymorphy3==2.0.2
pymorphy3-dicts-ru==2.4.417150.4580142
# Proxy connector для Telegram polling через aiogram AiohttpSession(proxy=...).
aiohttp-socks==0.9.0
python-socks[asyncio]==2.5.3
# Dostoevsky (тональность RuSentiment, провайдер анализа). Сам пакет ставится в
# Dockerfile с --no-deps (его пин fasttext==0.9.2 не собирается на py3.12); вместо
# него — готовые бинарники fasttext-wheel + токенизатор razdel. numpy<2 — fasttext.
fasttext-wheel==0.9.2
razdel==0.5.0
numpy<2
