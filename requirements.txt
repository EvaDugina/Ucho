# Дополнительные / новые зависимости (быстрый слой). Базовые тяжёлые ядро-либы —
# в requirements-base.txt (ставятся раньше и кэшируются отдельно).
# Инструментальный сигнал тональности (Фаза E): VADER (англ. лексикон) + локальный
# офлайн-перевод RU→EN Argos Translate (модель ru→en ставится в Dockerfile на build).
vaderSentiment==3.3.2
argostranslate==1.9.6
