# Дополнительные / новые зависимости (быстрый слой). Базовые тяжёлые ядро-либы — в
# requirements-base.txt (ставятся раньше и кэшируются отдельно). Сюда — лёгкие/часто
# меняемые либы.
# Лемматизация русского ввода для VAD-лексикона (bot/lexicon.py): нативно по-русски,
# CPU, офлайн. Заменяет связку перевод(argos)→VADER. dicts-ru — словарная база.
pymorphy3==2.0.2
pymorphy3-dicts-ru==2.4.417150.4580142
